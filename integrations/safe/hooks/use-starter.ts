import { useState } from 'react'

/**
 * Placeholder hook for starter. Replace with your own hook.
 */
export function useStarter() {
  const [starter, setStarter] = useState()
  return { starter, setStarter }
}
